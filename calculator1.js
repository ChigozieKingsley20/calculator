

//arithmetic oparations
function add(){
 fat = parseInt(myform.fat.value);
 fit = parseInt(myform.fit.value);
 result = fat + fit;
  }

function subtract(){
    fat = parseInt(myform.fat.value);
    fit = parseInt(myform.fit.value);
    result = fat - fit;
  }   

function multiply(){
    fat = parseInt(myform.fat.value);
    fit = parseInt(myform.fit.value);
    result = fat * fit;
  }   

function divide(){
    fat = parseInt(myform.fat.value);
    fit = parseInt(myform.fit.value);
    result = fat / fit;
  }   

function remainder(){
 fat = parseInt(myform.fat.value);
 fit = parseInt(myform.fit.value);
 result = fat % fit;

  } 


//functions to display the arithmetic calculations
  function calculate() {
  if(  myform.result.value = result) {
    result = fat % fit;
  }
else{
}
  }


  function calculate() {
    if(  myform.result.value = result) {
      result = fat + fit;
    }
  else{
  }
    }


  
    
  function calculate() {
    if(  myform.result.value = result) {
      result = fat * fit;
    }
  else{
  }
    }


    
  function calculate() {
    if(  myform.result.value = result) {
      result = fat / fit;
    }
  else{
  }
    }
